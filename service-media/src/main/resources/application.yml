server:
  port: 0
spring:
  application:
    name: service-media
  cloud:
    consul:
      host: ${CONSUL_HOST}
      port: ${CONSUL_PORT}
    config:
      discovery:
        enabled: true
        service-id: config
  config:
    import: optional:configserver:config

